import { InputFoamtDirective } from './input-foamt.directive';

describe('InputFoamtDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFoamtDirective();
    expect(directive).toBeTruthy();
  });
});
